package groupna.projectNetflix;

import groupna.projectNetflix.DAO.*;
import groupna.projectNetflix.controllers.DataStore;
import groupna.projectNetflix.entities.*;
import java.time.LocalDate;
import java.time.LocalTime;
import java.util.*;

public class Main {
    /*public static void main(String[] args) {
    	for(Film f:DataStore.getMovies()) {
    		FilmDAO.save(f);
    	}
    	for(Serie s:DataStore.getSeries()) {
    		SerieDAO.save(s);
    	}
    }*/
    
}